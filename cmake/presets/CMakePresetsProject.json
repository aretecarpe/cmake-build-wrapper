{
	"version": 4,
	"configurePresets": [
		{
			"name": "custom_presets",
			"hidden": true,
			"binaryDir": "${sourceDir}/build",
			"installDir": "${sourceDir}/installed_artifacts",
			"cacheVariables": {
				"CMAKE_PATH": "${sourceDir}/cmake"
			}
		}
	]
}